import { makeStyles } from "@material-ui/core";

const useStyle = makeStyles((theme) => {
    return {
        textField: {
            width: "400px",
        },
        margin: {
            margin: "20px 0",
        },
    };
});

export default useStyle;
